# ローカル開発用は .env.local に dev の値を書く（gitignore 対象・手書き）。
# 本ファイルは変数名の雛形のみ。実値は書かない（仕様書 §2）。
# CI ではこれらを terraform output から取得して $GITHUB_ENV に流し込む。

NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_PORTAL_URL=
NEXT_PUBLIC_ENV=
